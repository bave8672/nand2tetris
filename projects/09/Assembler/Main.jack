class Main {
    function void main() {
        var Assembler assembler;
        let assembler = Assembler.new();
        do assembler.assemble();
        do assembler.dispose();
        return;
    }
}
